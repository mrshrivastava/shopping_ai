import { trace, flushAllTraces, Span, SpanStatus } from 'next/dist/trace/trace';
import type { SpanId } from 'next/dist/trace/shared';
import { setGlobal } from 'next/dist/trace/shared';
export { trace, flushAllTraces, Span, setGlobal, SpanStatus };
export type { SpanId };
