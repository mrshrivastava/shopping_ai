import type { ReactNode } from 'react';
import type { CacheNode } from 'next/dist/shared/lib/app-router-context.shared-runtime';
import type { FlightRouterState } from 'next/dist/server/app-render/types';
import { CacheStates } from 'next/dist/shared/lib/app-router-context.shared-runtime';
export interface InitialRouterStateParameters {
    buildId: string;
    initialTree: FlightRouterState;
    initialCanonicalUrl: string;
    children: ReactNode;
    initialParallelRoutes: CacheNode['parallelRoutes'];
    isServer: boolean;
    location: Location | null;
    initialHead: ReactNode;
}
export declare function createInitialRouterState({ buildId, initialTree, children, initialCanonicalUrl, initialParallelRoutes, isServer, location, initialHead, }: InitialRouterStateParameters): {
    buildId: string;
    tree: FlightRouterState;
    cache: {
        status: CacheStates.READY;
        data: null;
        head?: ReactNode;
        subTreeData: ReactNode;
        parallelRoutes: Map<string, import("next/dist/shared/lib/app-router-context.shared-runtime").ChildSegmentMap>;
    };
    prefetchCache: Map<any, any>;
    pushRef: {
        pendingPush: boolean;
        mpaNavigation: boolean;
    };
    focusAndScrollRef: {
        apply: boolean;
        onlyHashChange: boolean;
        hashFragment: null;
        segmentPaths: never[];
    };
    canonicalUrl: string;
    nextUrl: string | null;
};
